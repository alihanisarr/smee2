services:
  smee2:
    build: .
    image: smee2image:latest
    ports: 
      - "5000:5000"
    container_name: smee2

  nginx:
    image: nginx:1.25.3
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports: 
      - "9000:80"
    container_name: nginx

  



